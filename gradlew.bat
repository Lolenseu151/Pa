@echo off
REM Gradle start-up script for Windows (stub)
REM NOTE: This is a wrapper stub. You still need a valid gradle-wrapper.jar
REM in gradle\wrapper\ to run the wrapper. Run `gradle wrapper` on a machine
REM with Gradle to create the jar, or install Gradle and run the build directly.

SET DIRNAME=%~dp0
"%DIRNAME%gradle\wrapper\gradle-wrapper.jar" %*
